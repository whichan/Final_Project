#ifndef APP_MAIN_H
#define APP_MAIN_H

/**
 * @brief  애플리케이션 초기화 — PWM 시작, 모터 정지
 *         main.c의 USER CODE BEGIN 2 에서 호출
 */
void App_Init(void);

/**
 * @brief  애플리케이션 루프 — while(1) 내부에서 반복 호출
 */
void App_Run(void);

#endif /* APP_MAIN_H */
